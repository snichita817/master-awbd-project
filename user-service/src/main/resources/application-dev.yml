spring:
  datasource:
    url: jdbc:mysql://localhost:3306/financetracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: false
  thymeleaf:
    cache: false

services:
  finance-core-url: http://localhost:8081
  reporting-url: http://localhost:8082
